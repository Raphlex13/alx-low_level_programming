#include "main.h"
/**
 * _isaplpha - function to check if c is letter, lowercase or uppercase
 * @c: is the int that will use for teh argument of the function
 * Return: 0
 */
int -isalpha(int c)
{
	if ((c > 'a' && c <= 'z' || (c > 'A' && c <= 'Z'))
	{
	return (1);
	}
	else
	return (0);
}
